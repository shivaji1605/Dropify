name: crypto_guide
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0' # Using a compatible SDK range

dependencies:
  flutter:
    sdk: flutter
  
  fl_chart: ^0.68.0
  provider: ^6.0.5
  cupertino_icons: ^1.0.8
  
  # --- We are KEEPING google_fonts, as you requested ---
  google_fonts: ^6.2.1 
  
  video_player: ^2.10.0
  url_launcher: ^6.3.2
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  google_sign_in: ^6.2.1   
  shared_preferences: ^2.2.3
  youtube_player_flutter: ^9.0.1
  http: ^1.2.1
  firebase_storage: ^12.1.0
  image_picker: ^1.1.2
  cloud_firestore: ^5.0.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # --- THIS SECTION IS NOW CORRECTLY INDENTED ---
  assets:
    - assets/images/

  # --- REMOVED the broken local fonts section ---
  # We will use the google_fonts package instead.

